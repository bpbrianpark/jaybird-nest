# PyTorch CPU-only (installed separately in Dockerfile)
# torch>=2.0.0  # Installed via CPU-only wheel
# torchvision>=0.15.0  # Installed via CPU-only wheel

Pillow>=10.0.0
numpy>=1.24.0,<2.0.0

scikit-learn>=1.3.0  
tqdm>=4.65.0 

fastapi>=0.100.0
uvicorn[standard]>=0.23.0 
pydantic>=2.0.0

# Excluded for production (not needed for inference):
# jupyter>=1.0.0
# matplotlib>=3.7.0